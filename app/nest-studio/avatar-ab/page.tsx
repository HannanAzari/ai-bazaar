import { AvatarAbClient } from "./avatar-ab-client";

export const metadata = { title: "Avatar A/B · Identity Test · Nestudio Studio", robots: { index: false, follow: false } };

export default function AvatarAbPage() {
  return <AvatarAbClient />;
}
