// Back-compat re-export. The curated-example builder now lives in the M17 discovery
// model (lib/nest-discovery.ts) so Home + Explore + the discovery lib share one source.
export { templateToExample } from "@/lib/nest-discovery";
